#include "func.h"
#include <math.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    printf("\n");
    //Circle circle = createCircle();
    //print_circle(circle);
    // Triangle triangle = createTriangle();

    return 0;
}